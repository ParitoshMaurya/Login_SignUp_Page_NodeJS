extends layout

block content
  a(href='/home') Home
  h1= title
  p Welcome to #{title}
  -if(flag==1)
    .alert.alert-danger(role='alert') #{message}
  -if(flag==0)
    .alert.alert-success(role="alert") #{message}
  .row
    .col-sm-1 
    .col-sm-4
      include include/login
    .col-sm-2
    .col-sm-4
      include include/registeration
    .col-sm-1